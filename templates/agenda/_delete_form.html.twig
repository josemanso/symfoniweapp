<form method="post" action="{{ path('app_agenda_delete', {'id': agenda.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ agenda.id) }}">
    <button class="btn">Delete</button>
</form>
